---
# Patch to add toleration for memory-constrained node to StatefulSets
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: not-used
spec:
  template:
    spec:
      tolerations:
        - key: memory-constrained
          operator: Equal
          value: "true"
          effect: NoSchedule
