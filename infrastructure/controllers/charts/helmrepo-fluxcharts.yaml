apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-charts
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/xrs444/flux.git   # <-- update if needed
  ref:
    branch: main
  # optionally, specify a path if your charts are not at the repo root
  # ignore: |
  #   !charts/kanidm