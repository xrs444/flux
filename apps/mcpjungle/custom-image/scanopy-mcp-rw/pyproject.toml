[project]
name = "scanopy-mcp-rw"
version = "0.1.0"
description = "A read/write MCP server for Scanopy (network topology/discovery)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.3.0",
    "requests>=2.31.0",
]
