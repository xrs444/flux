---
apiVersion: v1
kind: Service
metadata:
  name: mcp-omada
  namespace: mcp-gateway
spec:
  selector:
    app: mcp-omada
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
