---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: All PV/PVC resources moved to respective app folders
# This kustomization kept for historical reference and backup label config
# The commonLabels below applied Longhorn backup labels to all PVCs

# Legacy backup configuration (now handled per-app):
# commonLabels:
#   recurring-job-group.longhorn.io/default: enabled

resources: []
