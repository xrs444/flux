apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atuin-data-pvc
  namespace: atuin
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
