apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: papercut-ng-data
  namespace: papercut-ng
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
