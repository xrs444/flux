apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cups-data
  namespace: cups
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
