---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: apprise-ntfy-credentials
  namespace: apprise
spec:
  encryptedData:
    # TODO: Seal this secret with the apprise user's ntfy password
    # Command to create and seal:
    # kubectl create secret generic apprise-ntfy-credentials \
    #   --from-literal=password='6WS4WsfOUJf3QpuJopEoI1K3luxu/tCShq1DQOTgWNI=' \
    #   --namespace apprise \
    #   --dry-run=client -o yaml | \
    #   kubeseal --format=yaml > \
    #     flux/apps/apprise/sealedsecret-apprise-ntfy-credentials.yaml
    password: REPLACE_WITH_SEALED_PASSWORD
  template:
    metadata:
      name: apprise-ntfy-credentials
      namespace: apprise
    type: Opaque
