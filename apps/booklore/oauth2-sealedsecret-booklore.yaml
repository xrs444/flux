---
apiVersion: bitnami.com/v1alpha1
# yamllint disable rule:line-length
# Seal with: kubeseal --format yaml < secret.yaml > oauth2-sealedsecret-booklore.yaml
# Source secret:
#   apiVersion: v1
#   kind: Secret
#   metadata:
#     name: oauth2-secret-booklore
#     namespace: booklore
#   type: Opaque
#   stringData:
#     client-id: oauth2_booklore
#     client-secret: <value from kanidm provisioning>
kind: SealedSecret
metadata:
  name: oauth2-secret-booklore
  namespace: booklore
spec:
  encryptedData:
    client-id: PLACEHOLDER_SEAL_THIS
    client-secret: PLACEHOLDER_SEAL_THIS
  template:
    metadata:
      name: oauth2-secret-booklore
      namespace: booklore
    type: Opaque
