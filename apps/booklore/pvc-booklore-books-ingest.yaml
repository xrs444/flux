---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booklore-books-ingest
  namespace: booklore
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""  # Empty string to prevent dynamic provisioning
  volumeName: booklore-books-ingest-nfs
  resources:
    requests:
      storage: 10Ti
