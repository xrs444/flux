apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "omada-data-pvc"
  namespace: omada
spec:
  storageClassName: "longhorn-retain"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi