---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: romm-db
  namespace: romm
spec:
  encryptedData:
    # Create this secret with:
    # kubectl create secret generic romm-db \
    #   --from-literal=password='<random-password>' \
    #   --from-literal=root-password='<random-root-password>' \
    #   --namespace romm --dry-run=client -o yaml | \
    # kubeseal --controller-name=sealed-secrets \
    #   --controller-namespace=kube-system -o yaml
    password: REPLACE_WITH_ENCRYPTED_SECRET
    root-password: REPLACE_WITH_ENCRYPTED_SECRET
  template:
    type: Opaque
