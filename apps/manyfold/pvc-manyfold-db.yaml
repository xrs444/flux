apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: manyfold-db
  namespace: manyfold
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-retain
  resources:
    requests:
      storage: 5Gi
