---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: immich-oauth2
  namespace: immich
spec:
  encryptedData:
    # Create this secret with:
    # kubectl create secret generic immich-oauth2 \
    #   --from-literal=client-secret='<kanidm-client-secret>' \
    #   --namespace immich --dry-run=client -o yaml | \
    # kubeseal --controller-name=sealed-secrets \
    #   --controller-namespace=kube-system -o yaml
    client-secret: REPLACE_WITH_ENCRYPTED_SECRET
  template:
    type: Opaque
