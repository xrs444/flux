---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: immich-db
  namespace: immich
spec:
  encryptedData:
    # Create this secret with:
    # kubectl create secret generic immich-db \
    #   --from-literal=password='<random-password>' \
    #   --namespace immich --dry-run=client -o yaml | \
    # kubeseal --controller-name=sealed-secrets \
    #   --controller-namespace=kube-system -o yaml
    password: REPLACE_WITH_ENCRYPTED_SECRET
  template:
    type: Opaque
