apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lubelogger-data-pvc
  namespace: lubelogger
spec:
  storageClassName: "longhorn-retain"
  volumeName: "lubelogger-pv-static"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi