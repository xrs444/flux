---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: romm-ingest
  namespace: romm
spec:
  storageClassName: ""
  volumeName: romm-ingest-nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
