---
apiVersion: v1
kind: Service
metadata:
  name: mcp-caldav-s
  namespace: mcp-personal
spec:
  selector:
    app: mcp-caldav-s
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
