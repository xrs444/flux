---
apiVersion: v1
kind: Service
metadata:
  name: powerdns-api
  namespace: xlab-mgmt
  labels:
    app: powerdns
spec:
  type: ClusterIP
  ports:
    - name: api
      port: 8081
      targetPort: api
      protocol: TCP
  selector:
    app: powerdns
