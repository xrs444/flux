# yamllint disable rule:line-length
# This file is no longer the Flux entry point. Apps are reconciled via the
# group kustomizations in flux/cluster/apps.yaml (apps-core, apps-observability,
# apps-media, apps-system-services, apps-services, apps-automation).
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
