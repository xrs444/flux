# yamllint disable rule:line-length
# Superseded by group kustomizations in flux/apps/{core,observability,media,...}/.
# Remove the apps Flux Kustomization (cluster/apps.yaml) once all groups are healthy.
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
