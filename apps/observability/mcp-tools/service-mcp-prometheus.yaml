---
apiVersion: v1
kind: Service
metadata:
  name: mcp-prometheus
  namespace: mcp-tools
spec:
  selector:
    app: mcp-prometheus
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
