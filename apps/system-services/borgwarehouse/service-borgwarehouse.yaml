---
apiVersion: v1
kind: Service
metadata:
  name: borgwarehouse
  namespace: borgwarehouse
spec:
  selector:
    app: borgwarehouse
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
  type: ClusterIP
