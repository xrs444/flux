apiVersion: v1
kind: Service
metadata:
  name: cups
  namespace: cups
spec:
  selector:
    app: cups
  ports:
    - name: http
      protocol: TCP
      port: 631
      targetPort: 631
    - name: lpd
      protocol: TCP
      port: 515
      targetPort: 515
  type: ClusterIP
