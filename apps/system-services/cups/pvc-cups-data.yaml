apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cups-data
  namespace: cups
  labels:
    recurring-job-group.longhorn.io/default: enabled
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
