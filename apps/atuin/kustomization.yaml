# Atuin - Shell history sync server
# Enables command history synchronization across multiple machines
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - sealedsecret-atuin-postgresql.yaml
  - helmrelease.yaml
  - ingressroute-atuin.yaml
  # Storage resources
  - pvc-atuin-data.yaml
