---
apiVersion: v1
kind: Service
metadata:
  name: nocodb
  namespace: nocodb
spec:
  selector:
    app: nocodb
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3000  # Route to auth-shim sidecar
  type: ClusterIP
