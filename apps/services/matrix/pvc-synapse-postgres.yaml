---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synapse-postgres-data
  namespace: matrix
spec:
  storageClassName: longhorn-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
