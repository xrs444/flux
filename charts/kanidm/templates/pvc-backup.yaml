apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kanidm-backup
  namespace: {{ .Release.Namespace }}
spec:
  storageClassName: {{ .Values.storageClassName | quote }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
