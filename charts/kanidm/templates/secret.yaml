# charts/kanidm/templates/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: kanidm-admin-secret
  namespace: {{ .Values.namespace }}
stringData:
  password: {{ .Values.adminPassword | quote }}